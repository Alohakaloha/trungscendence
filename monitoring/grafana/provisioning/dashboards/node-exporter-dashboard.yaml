apiVersion: 1

providers:
  - name: NodeExporter # A uniquely identifiable name for the provider
    folder: Trungscendence # The folder where to place the dashboards
    type: file
    options:
      path:
        /var/lib/grafana/dashboards/